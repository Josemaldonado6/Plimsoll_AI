# -----------------------------------------------------------------------------
# requirements.txt (RAÍZ) — SOLO para Vercel / Cloud
#
# NO incluye: openvino-dev, nncf, ultralytics, torch, transformers,
#             paddlepaddle, paddleocr, timm, accelerate, watchdog, pymodbus
#
# Esos paquetes están en backend/requirements.txt para el dispositivo Edge.
# -----------------------------------------------------------------------------

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Database — SQLite (local/Edge) o PostgreSQL (Vercel Postgres)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
asyncpg>=0.29.0

# Utils
numpy>=1.26.0
Pillow>=10.0.0
reportlab>=4.1.0
beautifulsoup4>=4.12.0
requests>=2.31.0
scipy>=1.12.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
